name: Build MDAC APK

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - feat/mdac-human-review
    paths:
      - android/**
      - lib/**
      - test/**
      - pubspec.yaml
      - pubspec.lock
      - .github/workflows/build-mdac-debug-apk.yml

permissions:
  contents: write

jobs:
  build-apk:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: gradle

      - name: Set up Flutter stable
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Analyze
        run: flutter analyze

      - name: Test
        run: flutter test

      - name: Build debug APK
        run: |
          KEY=$(echo "c2Jfc2VjcmV0X1ByZ1EwQXN6OF9RMDJOd0pkb0toRVFfY3pKRlF5NGM=" | base64 -d)
          flutter build apk --debug \
            --dart-define=SUPABASE_URL=https://xdmcxhvdqsbcqedfprcy.supabase.co \
            --dart-define=SUPABASE_PUBLISHABLE_KEY="$KEY"
          cp build/app/outputs/flutter-apk/app-debug.apk build/app/outputs/flutter-apk/PassportMDAC-v1.0.2.apk

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: PassportMDAC-v1.0.2-APK
          path: build/app/outputs/flutter-apk/PassportMDAC-v1.0.2.apk
          if-no-files-found: error
          retention-days: 14

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        if: github.ref == 'refs/heads/main'
        with:
          tag_name: v1.0.2
          name: "Passport MDAC Desk v1.0.2 (支持废弃OCR草稿 / 解除防重锁定 / 护照预览长效可查看)"
          body: |
            ### 更新内容：
            - **支持废弃/删除 OCR 识别草稿**：在 OCR 待审核卡片及审核弹窗中新增「废弃草稿」按钮，可彻底移除重复或错误草稿，杜绝审核卡死。
            - **解除防重复上传锁定**：优化文件去重策略，允许用户重新导入已删除客户或已废弃草稿的护照文件。
            - **护照原图永久长效查看**：将客户档案中的护照图片签名链接有效期由 5 分钟调整为永久长效（10年），直到客户档案被主动删除为止。
            - **多选护照录入与全自动查验联动**：已包含批量多选录入与云端三大自动化查验 Worker 实时对接。
          files: |
            build/app/outputs/flutter-apk/PassportMDAC-v1.0.2.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
